TSE_EventManagerConfig {
 m_fEventCheckInterval 300 // Check for new events every 5 minutes
 m_iMaxSimultaneousEvents 2 // Maximum 2 events at the same time
 m_iMinGlobalCooldown 1 // Minimum 1 hour between any events
 m_bEventSystemEnabled 1 // Enable event system
 m_iInitialDelayMinutes 2 // Wait 2 minutes after game start before first event
 m_bDebugLogging 1 // Enable debug logging
} 